import { BackgroundWrapper } from '@components';
import { AlbumList } from '@screens';

const Album = () => {
  return (
    <BackgroundWrapper>
      <AlbumList />
    </BackgroundWrapper>
  );
};

export default Album;
