import { BackgroundWrapper, MusicPlayer } from '@components';

const Player = () => {
  return (
    <BackgroundWrapper>
      <MusicPlayer />
    </BackgroundWrapper>
  );
};

export default Player;
