import { BackgroundWrapper } from '@components';
import { MusicPlayer } from '@screens';

const Player = () => {
  return (
    <BackgroundWrapper>
      <MusicPlayer />
    </BackgroundWrapper>
  );
};

export default Player;
