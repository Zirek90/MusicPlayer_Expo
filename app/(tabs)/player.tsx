import { Text } from 'react-native';
import { BackgroundWrapper } from '@components';

const Player = () => {
  return (
    <BackgroundWrapper>
      <Text>Player</Text>
    </BackgroundWrapper>
  );
};

export default Player;
