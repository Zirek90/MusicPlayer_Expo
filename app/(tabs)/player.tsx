import { BackgroundWrapper } from '@components';
import { Text } from 'react-native';

const Player = () => {
  return (
    <BackgroundWrapper>
      <Text>Player</Text>
    </BackgroundWrapper>
  );
};

export default Player;
